# This file is part of the Sylius package.
# (c) Paweł Jędrzejewski

imports:
    - { resource: "@SyliusApiBundle/Resources/config/grids/product.yml" }

sylius_api:
    resources:
        api_user:
            classes:
                model: "%sylius.model.admin_user.class%"
