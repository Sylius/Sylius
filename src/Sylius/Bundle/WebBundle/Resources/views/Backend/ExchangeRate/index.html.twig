{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/ExchangeRate:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.exchange_rate.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_exchange_rate_create'), 'sylius.exchange_rate.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-usd"></i> {{ 'sylius.exchange_rate.index_header'|trans|raw }}</h1>
</div>

{{ list(exchange_rates) }}

{% endblock %}
