{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li><a href="{{ path('sylius_backend_shipping_method_index') }}">{{ 'sylius.breadcrumb.report.index'|trans }}</a></li>
    <li>{{ report.name }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.manage(path('sylius_backend_report_index'), 'sylius.report.manage'|trans) }}
        {{ buttons.edit(path('sylius_backend_report_update', {'id': report.id})) }}
        {{ buttons.delete(path('sylius_backend_report_delete', {'id': report.id}), null, false, false) }}
    </div>
    <h1><i class="glyphicon glyphicon-cog"></i> {{ 'sylius.report.show_header'|trans|raw }}</h1>
</div>


<table class="table table-bordered table-condensed">
    <thead>
        <tr>
            <th>{{ 'sylius.report.id'|trans }}</th>
            <th>{{ 'sylius.report.name'|trans }}</th>
            <th>{{ 'sylius.report.description'|trans }}</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ report.id }}</td>
            <td>{{ report.name }}</td>
            <td>{{ report.description }}</td>
        </tr>
    </tbody>
</table>
{% endblock %}
