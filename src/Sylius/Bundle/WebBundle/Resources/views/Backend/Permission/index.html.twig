{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Permission:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.customers'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.permission.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_permission_create'), 'sylius.permission.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-lock"></i> {{ 'sylius.permission.index_header'|trans|raw }}</h1>
</div>

{{ pagination(permissions) }}
{{ list(permissions) }}
{{ pagination(permissions) }}

{% endblock %}
