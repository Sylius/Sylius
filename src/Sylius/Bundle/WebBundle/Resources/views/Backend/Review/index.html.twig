{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Review:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.review.index'|trans }}</li>
    <li>{{ 'sylius.review.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_product_review_create'), 'sylius.review.create'|trans) }}
        {{ buttons.manage(path('sylius_backend_product_review_index'), 'sylius.review.manage'|trans) }}
    </div>
    <h1><i class="icon-th"></i> {{ 'sylius.review.index'|trans }}</h1>
</div>

{{ pagination(product_reviews) }}
{{ list(product_reviews) }}
{{ pagination(product_reviews) }}

{% endblock %}
