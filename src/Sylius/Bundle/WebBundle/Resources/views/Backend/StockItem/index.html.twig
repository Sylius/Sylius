{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/StockItem:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.stock_item.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.manage(path('sylius_backend_stock_location_index'), 'Manage stock locations') }}
    </div>
    <h1><i class="glyphicon glyphicon-cog"></i> {{ 'sylius.stock_item.index_header'|trans|raw }}</h1>
</div>

{{ pagination(stock_items) }}
{{ list(stock_items) }}
{{ pagination(stock_items) }}

{% endblock %}
