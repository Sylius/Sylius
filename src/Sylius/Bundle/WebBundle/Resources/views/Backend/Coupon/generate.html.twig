{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.ui.sales'|trans }}</li>
    <li><a href="{{ path('sylius_backend_promotion_index') }}">{{ 'sylius.ui.promotions'|trans }}</a></li>
    <li><a href="{{ path('sylius_backend_promotion_show', {'id': promotion.id}) }}">{{ promotion.name }}</a></li>
    <li><a href="{{ path('sylius_backend_promotion_coupon_index', {'promotionId': promotion.id}) }}">{{ 'sylius.ui.coupons'|trans }}</a></li>
    <li>{{ 'sylius.ui.generate'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <h1><i class="glyphicon glyphicon-random"></i> {{ 'sylius.ui.generating_coupons'|trans|raw }}</h1>
</div>

{{ form_errors(form) }}

<form action="{{ path('sylius_backend_promotion_coupon_generate', {'promotionId': promotion.id}) }}" method="post" class="form-horizontal" novalidate>
    {% include 'SyliusWebBundle:Backend/Coupon:_generateForm.html.twig' %}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary btn-lg"><i class="glyphicon glyphicon-random"></i> {{ 'sylius.ui.generate'|trans }}</button>
        <a href="{{ app.request.headers.get('referer') }}" class="btn btn-lg"><i class="glyphicon glyphicon-remove"></i> {{ 'sylius.ui.cancel'|trans }}</a>
    </div>
</form>
{% endblock %}
