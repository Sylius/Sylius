{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% set promotion = promotion_coupon.promotion %}

{% from 'SyliusUiBundle::forms.html.twig' import update %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.sales'|trans }}</li>
    <li><a href="{{ path('sylius_backend_promotion_index') }}">{{ 'sylius.breadcrumb.promotion.index'|trans }}</a></li>
    <li><a href="{{ path('sylius_backend_promotion_update', {'id': promotion.id}) }}">{{ promotion.name }}</a></li>
    <li><a href="{{ path('sylius_backend_promotion_coupon_index', {'promotionId': promotion.id}) }}">{{ 'sylius.breadcrumb.promotion_coupon.index'|trans }}</a></li>
    <li>{{ promotion_coupon.code }}</li>
    <li>{{ 'sylius.breadcrumb.update'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
{{ form_start(form, {'action': path('sylius_backend_promotion_coupon_update', {'id': promotion_coupon.id, 'promotionId': promotion.id}), 'method': 'POST', 'attr': {'class': 'form-horizontal'}}) }}

{{ page.header('promotion_coupon.update', 'pencil', 'SyliusUiBundle:Form/Actions:_update.html.twig') }}

{{ form_errors(form) }}
{% include 'SyliusWebBundle:Backend/Coupon:_form.html.twig' %}
<input type="hidden" name="_method" value="PUT">

{{ form_end(form) }}
{% endblock %}
