
{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Content/Route:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.content'|trans }}</li>
    <li><a>{{ 'sylius.breadcrumb.route.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_route_create'), 'sylius.route.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-random"></i> {{ 'sylius.route.index_header'|trans|raw }}</h1>
</div>

{{ pagination(routes) }}
{{ list(routes) }}
{{ pagination(routes) }}

{% endblock %}
