{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Email:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.marketing'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.email.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_email_create'), 'sylius.email.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-envelope"></i> {{ 'sylius.email.index_header'|trans|raw }}</h1>
</div>

{{ pagination(emails) }}
{{ list(emails) }}
{{ pagination(emails) }}

{% endblock %}
