{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}


{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.assortment'|trans }}</li>
    <li><a href="{{ path('sylius_backend_product_archetype_index') }}">{{ 'sylius.breadcrumb.archetype.index'|trans }}</a></li>
    <li>{{ 'sylius.breadcrumb.build'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <h1>{{ 'sylius.archetype.build_header'|trans|raw }}</h1>
</div>

<form action="{{ path('sylius_backend_product_archetype_build', {'id': product_archetype.id}) }}" method="post" class="form-horizontal" {{ form_enctype(form) }} novalidate>
    {% include 'SyliusWebBundle:Backend/Product:_form.html.twig' %}
    <div class="form-actions">
        <input type="submit" value="{{ 'sylius.create'|trans }}" class="btn btn-primary btn-lg" />
        <a href="{{ path('sylius_backend_product_archetype_index') }}" class="btn btn-lg">{{ 'sylius.cancel'|trans }}</a>
    </div>
</form>
{% endblock %}
