{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/AffiliateBanner:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.marketing'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.affiliate.banner.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_affiliate_banner_create'), 'sylius.affiliate.banner.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-bullhorn"></i> {{ 'sylius.affiliate.banner.index_header'|trans|raw }}</h1>
</div>

{{ pagination(affiliate_banners) }}
{{ list(affiliate_banners) }}
{{ pagination(affiliate_banners) }}

{% endblock %}
