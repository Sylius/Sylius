{% from 'SyliusWebBundle:Frontend/Macros:actions.html.twig' import update %}
{% extends 'SyliusWebBundle:Frontend/Account:layout.html.twig' %}

{% block content %}
    <h4>{{ 'sylius.account.subscription.update'|trans }}</h4>

    {{ form_errors(form) }}
    {{ form_start(form, { method: 'PUT' }) }}
    {% include 'SyliusWebBundle:Frontend/Account/Subscription:_form.html.twig' %}
    {{ update() }}
    {{ form_end(form) }}

{% endblock %}
