<address>
    <strong>{{ address.firstName }} {{ address.lastName }}</strong><br/>
    {% if address.company %}
        {{ address.company }}<br/>
    {% endif %}
    {{ address.phoneNumber }}<br/>
    {{ address.street }}<br/>
    {{ address.city }}<br/>
    {% if address.province %}
        {{ address.province }}<br/>
    {% endif %}
    {{ address.country|sylius_country_name|upper }} {{ address.postcode }}
</address>
