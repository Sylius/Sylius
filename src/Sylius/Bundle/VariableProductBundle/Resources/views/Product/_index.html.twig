{% import 'SyliusVariableProductBundle:Product:macros.html.twig' as macros %}

<h1>Products list <small>All products saved in the system</small></h1>
<hr />

<div class="well">
    <a href="{{ path('sylius_product_create') }}" class="btn btn-primary">
        <i class="icon-plus-sign"></i> create product
    </a>
</div>

{% if products.haveToPaginate()|default(false) %}
    {{ pagerfanta(products, 'twitter_bootstrap_translated') }}
{% endif %}

{{ macros.list(products) }}

{% if products.haveToPaginate()|default(false) %}
    {{ pagerfanta(products, 'twitter_bootstrap_translated') }}
{% endif %}
