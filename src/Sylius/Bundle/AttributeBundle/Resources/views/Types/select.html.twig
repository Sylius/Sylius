{% if attribute.value is not null %}
    {% set options = attribute.selectOptions %}
    {% for option in options %}
        {{ option.name }}{% if loop.last == false %}, {% endif %}
    {% endfor %}
{% endif %}