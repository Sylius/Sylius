{% set format = attribute.attribute.configuration['format']|default(null) %}

{% if format is not empty %}
    {{ attribute.value|format_date(format) }}
{% else %}
    {{ attribute.value|format_date }}
{% endif %}
