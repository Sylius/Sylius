{% if attribute.attribute.configuration['format'] is defined %}
    <td id="sylius_attribute_value">{{ attribute.value|date(attribute.attribute.configuration['format']) }}</td>
{% else %}
    <td id="sylius_attribute_value">{{ attribute.value|date }}</td>
{% endif %}
