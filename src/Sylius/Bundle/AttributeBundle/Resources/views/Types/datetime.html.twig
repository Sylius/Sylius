{% if attribute.attribute.configuration['format'] is defined %}
    <td>{{ attribute.value|date(attribute.attribute.configuration['format']) }}</td>
{% else %}
    <td>{{ attribute.value|date('d/m/Y H:i:s') }}</td>
{% endif %}
