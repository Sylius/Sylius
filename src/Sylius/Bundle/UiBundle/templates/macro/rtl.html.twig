{% macro default(locale) %}
    {% set items = ['ar'] %}

    {% for item in items %}
        {% if locale starts with item %}
            dir="rtl"
        {% endif %}
    {% endfor %}
{% endmacro %}
