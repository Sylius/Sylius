<h1>Taxation category details <small>{{ tax_category.name }}</small></h1>
<hr>
<div class="well">
    <a href="{{ path('sylius_tax_category_index') }}" class="btn btn-primary">
        <i class="icon-list-alt"></i> manage taxation categories
    </a>
    <a href="{{ path('sylius_tax_category_update', {'id': tax_category.id}) }}" class="btn">
        <i class="icon-pencil"></i> edit
    </a>
    <a href="{{ path('sylius_tax_category_delete', {'id': tax_category.id}) }}" class="btn btn-danger">
        <i class="icon-trash"></i>
    </a>
</div>

<div class="well">
{{ tax_category.description|default('No description') }}
</div>
