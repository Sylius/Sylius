<h1>Editing tax rate <small>{{ rate.name }}</small></h1>
<hr />
<form action="{{ path('sylius_taxation_rate_update', {'id': rate.id}) }}" method="post" class="form-horizontal" novalidate>
    {% include 'SyliusTaxationBundle:TaxRate:_form.html.twig' %}
    <div class="form-actions">
        <input type="submit" value="save changes" class="btn btn-primary btn-large" />
        <a href="{{ path('sylius_taxation_rate_show', {'id': rate.id}) }}" class="btn btn-large">cancel</a>
    </div>
</form>
