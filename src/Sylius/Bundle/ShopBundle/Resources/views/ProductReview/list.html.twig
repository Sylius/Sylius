{% import '@SyliusShop/Macro/messages.html.twig' as messages %}

{% if product_reviews|length > 0 %}
    <div>
        {% for review in product_reviews %}
            {% include '@SyliusShop/ProductReview/single.html.twig' %}
        {% endfor %}
    </div>
{% else %}
    {{ messages.info('sylius.ui.there_are_no_reviews') }}
{% endif %}
