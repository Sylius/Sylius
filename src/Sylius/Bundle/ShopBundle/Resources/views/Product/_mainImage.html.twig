{% set path = product.imageByCode('thumbnail') is not null ? product.imageByCode('thumbnail').path|imagine_filter(filter|default('sylius_shop_product_thumbnail')) : 'http://placehold.it/200x200' %}

<img src="{{ path }}" alt="" class="ui bordered image" />
