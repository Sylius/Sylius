{% for payment in order.payments %}
<div class="ui large icon message">
    <i class="payment icon"></i>
    <div class="content">
        <div class="header">
            {{ payment.method }}
        </div>
        <p>{{ payment.amount|sylius_money(payment.currencyCode) }}</p>
    </div>
</div>
{% endfor %}
