<div class="ui segment">
    <div class="ui dividing header">{{ 'sylius.ui.shipment'|trans }} #{{ loop.index }}</div>
    <div class="ui fluid stackable items">
        {{ form_errors(form.method) }}

        {% for choice_form in form.method %}
            {% include '@SyliusShop/Checkout/SelectShipment/_choice.html.twig' with {'form': choice_form, 'method': form.method.vars.choices[loop.index0].data} %}
        {% else %}
            {% include '@SyliusShop/Checkout/SelectShipment/_unavailable.html.twig' %}
        {% endfor %}
    </div>
</div>
