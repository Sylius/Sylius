{% extends '@SyliusShop/layout.html.twig' %}

{% block content %}
    {% set lastNewPayment = order.getLastNewPayment() %}
    {% include '@SyliusShop/Common/Order/_summary.html.twig' %}

    {% if order.paymentState is not same as ('paid') %}
        {{ render(url('sylius_shop_order_payment_method_update', {'orderId': order.id, 'lastNewPaymentId': lastNewPayment.id} )) }}
    {% endif %}
{% endblock %}
