{% import 'SyliusUiBundle:Macro:buttons.html.twig' as buttons %}

{% if data.paymentState in ['awaiting_payment'] %}
    {% set lastNewPayment = data.getLastNewPayment() %}

    {{ buttons.default(path('sylius_shop_order_pay', { 'paymentId': lastNewPayment.id }), 'sylius.ui.pay'|trans) }}
{% endif %}
