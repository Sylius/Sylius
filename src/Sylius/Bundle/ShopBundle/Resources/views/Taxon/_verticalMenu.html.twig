<div class="ui fluid vertical menu">
    <div class="header item">{{ taxon.name }}</div>
    {% if taxon.parent is not empty and not taxon.parent.isRoot() %}
        <a href="{{ path('sylius_shop_product_index', {'slug': taxon.parent.slug}) }}" class="item">
            <i class="up arrow icon"></i> {{ 'sylius.ui.go_level_up'|trans }}
        </a>
    {% endif %}
    {% for child in taxon.children %}
    <a href="{{ path('sylius_shop_product_index', {'slug': child.slug}) }}" class="item">{{ child.name }}</a>
    {% endfor %}
</div>
