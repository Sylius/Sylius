{% set form = hookable_metadata.context.form %}

{% form_theme form '@SyliusShop/Form/theme.html.twig' %}

{{ form_start(form, {'action': path('sylius_shop_account_address_book_update', {'id': hookable_metadata.context.address.id}), 'method': 'PUT', 'attr': {'novalidate': 'novalidate'}}) }}

{% hook 'form' %}

{{ form_row(form._token) }}
{{ form_end(form, {'render_rest': false}) }}
