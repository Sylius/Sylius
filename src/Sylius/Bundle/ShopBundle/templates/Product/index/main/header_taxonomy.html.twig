{% if taxon.images.first %}
    <div class="mb-5">
        <img src="{{ asset(taxon.images.first.path|imagine_filter('sylius_shop_taxon_thumbnail')) }}"
             alt="{{ taxon.name }}"
             class="img-fluid"
        >
    </div>
{% endif %}
<h1 class="mb-3">{{ taxon.name }}</h1>
<div>{{ taxon.description }}</div>
