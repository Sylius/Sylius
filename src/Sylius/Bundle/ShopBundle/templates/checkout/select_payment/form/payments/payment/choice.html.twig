{% set form = hookable_metadata.context.form %}

<div class="mb-5">
    {{ form_errors(form.method) }}

    {% for key, choice_form in form.method %}
        {% hook 'choice' with { 'form': choice_form, 'method': form.method.vars.choices[key].data } %}
    {% else %}
        {% hook 'choice_unavailable' %}
    {% endfor %}
</div>
