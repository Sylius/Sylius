{% set form = hookable_metadata.context.form %}
{% set order = hookable_metadata.context.order %}
{% set type = hookable_metadata.context.type %}

{% if type == 'shipping' %}
    {% hook 'address' with { _prefixes: ['sylius_shop.shared.form'], 'form': form.shippingAddress, 'order': order, 'type': 'shipping' } %}
{% else %}
    {% hook 'address' with { _prefixes: ['sylius_shop.shared.form'], 'form': form.billingAddress, 'order': order, 'type': 'billing' } %}
{% endif %}
