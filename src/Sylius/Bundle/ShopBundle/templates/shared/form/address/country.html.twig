{% set form = hookable_metadata.context.form.countryCode %}

<div class="mb-3">
    {% if form.vars.choices|length == 1 %}
        {{ form_row(form, sylius_test_form_attribute(hookable_medatada.context.type|default(null) ~ 'country')|merge({'value': form.vars.choices[0].value})) }}
    {% else %}
        {{ form_row(form, sylius_test_form_attribute(hookable_medatada.context.type|default(null) ~ 'country')) }}
    {% endif %}
</div>
