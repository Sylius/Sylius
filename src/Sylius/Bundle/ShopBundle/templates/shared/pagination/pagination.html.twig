{% macro simple(paginator, options) %}
    {% if paginator.haveToPaginate() %}
        {{ pagerfanta(paginator, 'twig', options|default({'template': '@SyliusShop/shared/pagination/bootstrap_5.html.twig'})) }}
    {% endif %}
{% endmacro %}
