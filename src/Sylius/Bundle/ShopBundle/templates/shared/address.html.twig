{% set address = hookable_metadata.context.address %}

<address {{ sylius_test_html_attribute('address-context', "%s %s"|format(address.firstName, address.lastName)) }}>
    {% hook 'address' with { _prefixes: ['sylius_shop.shared'] } %}
</address>
