{% form_theme form '@SyliusShop/Form/theme.html.twig' %}

{% if form.type is defined %}
    {{ form_row(form.type, {'label': filter.label}) }}
{% endif %}
{{ form_widget(form.value, {attr: {placeholder: form.value.vars.label, class: 'form-control-sm'}, 'label': form.type is defined ? form.value.vars.label : filter.label}) }}
