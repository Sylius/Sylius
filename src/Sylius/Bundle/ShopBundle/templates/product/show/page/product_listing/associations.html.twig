{% set product = hookable_metadata.context.product %}

{% for association in product.associations %}
    {{ component('sylius_shop:product:association', { productAssociation: association }) }}
{% else %}
    {{ component('sylius_shop:homepage:latest_products', { limit: 4 }) }}
{% endfor %}
