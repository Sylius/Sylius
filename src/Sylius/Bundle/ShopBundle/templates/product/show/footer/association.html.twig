{% if not product_association.associatedProducts.empty() %}
    <div>{{ product_association.type.name }}</div>
    <div {{ sylius_test_html_attribute('product-association', product_association.type.name) }}>
        {% include '@SyliusShop/product/horizontal_list.html.twig' with {'products': product_association.associatedProducts} %}
    </div>
{% endif %}
