{%
    set viewOptions = {
    accepted: { color: 'bg-green-lt' },
    new: { color: '' },
    rejected: { color: 'bg-red-lt' },
}
%}

{% set value = 'sylius.ui.' ~ data %}

<span class="badge {{ viewOptions[data]['color'] }}">
    {{ value|trans }}
</span>
