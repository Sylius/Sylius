{% set form = hookable_data.form %}

<div class="tab-pane" id="product-variant-taxes" role="tabpanel" tabindex="0">
    <div class="card mb-3">
        <div class="card-header">
            <h2 id="taxes" class="card-title">{{ 'sylius.ui.taxes'|trans }}</h2>
        </div>
        <div class="card-body">
            {{ form_row(form.taxCategory) }}
        </div>
    </div>
</div>
