{% if data.isTracked %}
<div class="ui {{ data.onHand > 0 ? 'green' : 'red' }} icon label">
    <i class="cube icon"></i>
    <span id="sylius-{{ data.id }}-on-hand-quantity">{{ data.onHand }}</span> {{ 'sylius.ui.available_on_hand'|trans }}
    {% if data.onHold > 0 %}
    <div class="detail">
        <span id="sylius-{{ data.id }}-on-hold-quantity">{{ data.onHold }}</span> {{ 'sylius.ui.reserved'|trans }}
    </div>
    {% endif %}
</div>
{% else %}
    <span class="ui red label">
        <i class="remove icon"></i>
        {{ 'sylius.ui.not_tracked'|trans }}
    </span>
{% endif %}
