<div class="ui segment">
    {{ form_errors(form) }}
    {{ form_row(form.code) }}
    {{ form_row(form.enabled) }}
</div>
<div class="ui segment">
    <h4 class="ui dividing header">{{ 'sylius.ui.provinces'|trans }}</h4>
    {{ form_row(form.provinces, {'label': false}) }}
</div>
<div class="ui segment">
    <h4 class="ui dividing header">{{ 'sylius.ui.postcodes'|trans }}</h4>
    {{ form_row(form.postal_codes, {'label': false}) }}
</div>