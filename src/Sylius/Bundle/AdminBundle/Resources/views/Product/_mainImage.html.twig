{% set path = product.image is not null ? product.image.path|imagine_filter(filter|default('sylius_admin_product_thumbnail')) : 'http://placehold.it/50x50' %}

<img src="{{ path }}" alt="" class="ui bordered image" />
