{% set path = product.imageByCode('thumbnail') is not null ? product.imageByCode('thumbnail').path|imagine_filter(filter|default('sylius_admin_product_thumbnail')) : 'http://placehold.it/50x50' %}

<img src="{{ path }}" alt="" class="ui bordered image" />
