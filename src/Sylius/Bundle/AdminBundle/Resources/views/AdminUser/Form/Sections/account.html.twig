{% set main_hook = hook_name(hookable_data.parent_main_hook, 'account') %}
{% set fallback_hook = hook_name(hookable_data.parent_fallback_hook, 'account') %}

<div class="col-12 col-md-6">
    <div class="card">
        <div class="card-header">
            <div class="card-title">
                {{ 'sylius.ui.account'|trans }}
            </div>
        </div>
        <div class="card-body">
            {% hook [main_hook, fallback_hook] with hookable_data|merge({
                parent_main_hook: main_hook,
                parent_fallback_hook: fallback_hook,
            }) %}
        </div>
    </div>
</div>
