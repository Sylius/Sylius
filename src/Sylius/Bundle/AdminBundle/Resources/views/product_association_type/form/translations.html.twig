{% import '@SyliusAdmin/Shared/Helper/translations.html.twig' as _translations %}

<div class="tab-pane" id="product-translations" role="tabpanel" tabindex="0">
    <div class="bg-white mb-3">
        {% set body %}
            {% verbatim %}
                <div class="row"">
                    <div class="col-12">
                        {{ form_row(translationForm.name) }}
                    </div>
                </div>
            {% endverbatim %}
        {% endset %}
        {{ _translations.default(hookable_data.form.translations, body) }}
    </div>
</div>
