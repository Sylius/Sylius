{% for name, gateway in gatewayFactories %}
    {% set createRouteName = metadata.applicationName~'_admin_'~metadata.name~'_create' %}
    <a href="{{ path(createRouteName, { 'factory': name }) }}" id="{{ name }}" class="item">
        {{ gateway|trans }}
    </a>
{% endfor %}
