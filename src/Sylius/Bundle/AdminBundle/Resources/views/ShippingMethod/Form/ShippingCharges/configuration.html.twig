{% set form = hookable_data.form %}

{% if form.calculator.vars.value is not empty %}
    <div class="col-12">
        {% if form.configuration is defined %}
            {{ form_widget(form.configuration) }}
        {% else %}
            {% for name, calculatorConfigurationPrototype in form.vars.prototypes %}
                {% set chosenCalculator = 'calculators_' ~ form.calculator.vars.value %}
                {% if chosenCalculator == name %}
                    {{ form_widget(calculatorConfigurationPrototype) }}
                {% endif %}
            {% endfor %}
        {% endif %}
    </div>
{% endif %}
