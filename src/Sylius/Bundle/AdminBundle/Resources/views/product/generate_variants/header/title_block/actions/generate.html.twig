{% import '@SyliusAdmin/Shared/Helper/button.html.twig' as _button %}
{% set form = hookable_metadata.context.form %}

{{ _button.primary(sylius_test_form_attribute('generate-button')|merge({ text: 'sylius.ui.generate'|trans, type: 'submit', form: form.vars.id, disabled: form.variants is empty })) }}
