{{ form_errors(form) }}
<div class="ui grid">
    <div class="eight wide column">
        <h4 class="ui dividing header">{{ 'sylius.ui.promotion_information'|trans }}</h4>
        <div class="field">
            {{ form_row(form.code) }}
        </div>
        <div class="field">
            {{ form_row(form.name) }}
        </div>
    </div>
</div>
