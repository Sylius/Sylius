{% set main_hook = hook_name(hookable_data.parent_main_hook, 'configuration') %}
{% set fallback_hook = hook_name(hookable_data.parent_fallback_hook, 'configuration') %}

<div class="card mb-3">
    <div class="card-header">
        <div class="card-title">
            {{ 'sylius.ui.configuration'|trans }}
        </div>
    </div>
    <div class="card-body">
        <div class="row g-7">
            {% hook [main_hook, fallback_hook] with hookable_data|merge({
                parent_main_hook: main_hook,
                parent_fallback_hook: fallback_hook,
            }) %}

            <div class="col-12 col-md-6">
                {% hook [main_hook ~ '#left', fallback_hook] with hookable_data|merge({
                    parent_main_hook: main_hook,
                    parent_fallback_hook: fallback_hook,
                }) %}
            </div>

            <div class="col-12 col-md-6">
                {% hook [main_hook ~ '#right', fallback_hook] with hookable_data|merge({
                    parent_main_hook: main_hook,
                    parent_fallback_hook: fallback_hook,
                }) %}
            </div>
        </div>
    </div>
</div>
