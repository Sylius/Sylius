{% from '@SyliusAdmin/Shared/Helper/icon.html.twig' import icon %}
{% import '@SyliusAdmin/Shared/Helper/table.html.twig' as _table %}

{% set event_name = event_prefix ~ '.grid' %}
{% set resource_event_name = resource_event_prefix ~ '.grid' %}

<div class="page-body">
    <div class="container-xl">
        {{ sylius_template_event([resource_event_name, event_name], _context|merge({ event_prefix: event_name, resource_event_prefix: resource_event_name })) }}
    </div>
</div>
