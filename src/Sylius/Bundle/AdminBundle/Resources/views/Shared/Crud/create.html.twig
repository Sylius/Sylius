{% extends '@SyliusAdmin/Shared/Layout/base.html.twig' %}

{% set main_event = _context.metadata.applicationName ~ '.admin.' ~ _context.metadata.name ~ '.create' %}
{% set fallback_event = _context.metadata.applicationName ~ '.admin.' ~ 'create.body' %}

{% set header = configuration.vars.header|default(_context.metadata.applicationName ~ '.ui.' ~ _context.metadata.pluralName) %}

{% block title %}{{ header|trans }} | {{ parent() }}{% endblock %}

{% block body %}
    {{ twig_event(
        [main_event, fallback_event],
        {
            main_event: main_event,
            fallback_event: fallback_event,
            resource: resource,
            metadata: metadata,
            configuration: configuration,
            form: form,
        }
    ) }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ twig_event([main_event ~ '.stylesheets', fallback_event ~ '.stylesheets']) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ twig_event([main_event ~ '.javascripts', fallback_event ~ '.javascripts']) }}
{% endblock %}
