{% set main_hook = hook_name(hookable_data.parent_main_hook, 'customer') %}
{% set fallback_hook = hook_name(hookable_data.parent_fallback_hook, 'customer') %}

<div class="card mb-3">
    <div class="card-header">
        <div class="card-title">
            {{ (_context.title|default('sylius.ui.customer'))|trans }}
        </div>
    </div>
    <div class="card-body">
        <div>
            {% hook [main_hook, fallback_hook] with hookable_data|merge({
                parent_main_hook: main_hook,
                parent_fallback_hook: fallback_hook,
            }) %}
        </div>
    </div>
</div>
