{% import '@SyliusAdmin/Shared/Helper/breadcrumbs.html.twig' as _breadcrumbs %}

{% set index_url = path(
    _context.configuration.vars.index.route.name|default(_context.configuration.getRouteName('index')),
    _context.configuration.vars.index.route.parameters|default(_context.configuration.vars.route.parameters|default({}))
)
%}

{{ _breadcrumbs.default([
    { name: 'Dashboard', url: path('sylius_admin_dashboard'), active: false },
    { name: metadata.applicationName~'.ui.'~metadata.pluralName, url: index_url, active: false },
    { name: '#' ~ resource.number, active: true },
    { name: 'sylius.ui.show'|trans, active: false }
]) }}
