{% set order = hookable_data.resource %}
{% set shipment = hookable_data.shipment %}

{% if sm_can(shipment, 'ship', 'sylius_shipment') %}
{# TODO: probably convert the partial into a live component #}
{#    {{ render(path('sylius_admin_partial_shipment_ship', {'orderId': order.id, 'id': shipment.id})) }}#}
{% endif %}
