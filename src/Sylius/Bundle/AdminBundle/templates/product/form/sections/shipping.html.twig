{% set product = hookable_metadata.context.resource %}

{% if product.isSimple %}
    <div class="tab-pane {% if hookable_metadata.configuration.active|default(false) %}show active{% endif %}" id="shipping" role="tabpanel" tabindex="0">
        <div class="card mb-3">
            <div class="card-header">
                <h2 id="shipping" class="card-title">{{ 'sylius.ui.shipping'|trans }}</h2>
            </div>
            <div class="card-body">
                {% hook 'shipping' %}
            </div>
        </div>
    </div>
{% endif %}
