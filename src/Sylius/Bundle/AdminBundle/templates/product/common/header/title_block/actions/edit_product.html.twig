{% from '@SyliusAdmin/shared/helper/icon.html.twig' import icon as _icon %}

{% set product = hookable_metadata.context.resource %}

<td class="text-end">
    <a href="{{ path('sylius_admin_product_update', { id: product.id }) }}" class="btn" {{ sylius_test_html_attribute('edit-product-button') }}>
        {{ _icon({ icon: 'pencil' }) }}
        {{ 'sylius.ui.edit'|trans }}
    </a>
</td>
