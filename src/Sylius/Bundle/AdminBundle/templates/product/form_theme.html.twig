{% extends '@SyliusAdmin/shared/form_theme.html.twig' %}

{% block _sylius_product_channels_row %}
    {{ form_label(form) }}

    {% for channel in form %}
        {{ form_row(channel, sylius_test_form_attribute('channel-name', channel.vars.label)|merge({ label_attr: { class: 'checkbox-switch' } })) }}
    {% endfor %}

    {{ form_errors(form) }}
{% endblock %}
