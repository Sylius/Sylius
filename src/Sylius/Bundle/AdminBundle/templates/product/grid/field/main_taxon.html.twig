{% if data.fullname is defined %}
    {{ data.fullname }}
{% else %}
    {% from '@SyliusAdmin/shared/helper/icon.html.twig' import icon as _icon %}

    <span class="text-danger">
        {{ _icon({ icon: 'exclamation-circle' }) }} <i>{{ 'sylius.ui.no_main_taxon'|trans }}</i>
    </span>
{% endif %}
