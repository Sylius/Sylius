{% set currency_code = hookable_metadata.context.currency_code %}
{% set channel_pricing = hookable_metadata.context.channel_pricing %}
{% set channel = hookable_metadata.context.channel %}
{% set variant = hookable_metadata.context.variant %}
{% import '@SyliusAdmin/shared/helper/money.html.twig' as _money %}

<td class="text-end" {{ sylius_test_html_attribute('lowest-price-before-the-discount', '%s.%s'|format(variant.name, channel.name)) }}>{% if channel_pricing.lowestPriceBeforeDiscount != null %}{{ _money.format(channel_pricing.lowestPriceBeforeDiscount, currency_code) }}{% else %} - {% endif %}</td>
