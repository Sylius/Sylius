<div class="row">
    <div class="col-12 col-md-4 fw-bold">{{ 'sylius.ui.phone'|trans }}</div>
    <div class="col-12 col-md-8" {{ sylius_test_html_attribute('customer-phone') }}>
        {% if hookable_metadata.context.customer.phoneNumber is not empty %}
            <a href="tel:{{ hookable_metadata.context.customer.phoneNumber }}">{{ hookable_metadata.context.customer.phoneNumber }}</a>
        {% else %}
            -
        {% endif %}
    </div>
</div>
