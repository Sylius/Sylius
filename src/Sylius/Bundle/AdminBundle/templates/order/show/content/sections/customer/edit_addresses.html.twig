{% set order = hookable_metadata.context.resource %}

<div class="mb-3">
    <a href="{{ path('sylius_admin_order_update', { id: order.id }) }}" class="btn btn-primary btn-sm w-100">
        {{ 'sylius.ui.edit_addresses'|trans }}
    </a>
</div>
