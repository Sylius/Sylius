# This file is part of the Sylius package.
# (c) Sylius Sp. z o.o.

sylius:
    form:
        rule:
            unit_count_configuration:
                count: Count
                equal: Equal to
            type: Rule type
        shipment:
            state: State
            states:
                checkout:   Checkout
                pending:    Pending
                ready:      Ready
                shipped:    Shipped
                returned:   Returned
                cancelled:  Cancelled
            tracking_code: Tracking Code
        shipping_calculator:
            flat_rate_configuration:
                label: Flat rate per shipment
                amount: Amount
            per_unit_rate_configuration:
                label: Flat rate per unit
                amount: Amount
        shipping_category:
            description: Description
            name: Name
        shipping_method:
            add_rule: Add rule
            calculator: Calculator
            category: Category
            category_requirement: Category requirement
            channels: Channels
            configuration: Configuration
            description: Description
            enabled: Enabled
            match_all_category_requirement: All units has to match the method category
            match_any_category_requirement: At least 1 unit has to match the method category
            match_none_category_requirement: None of the units have to match the method category
            name: Name
            position: Position
            rules: Rules
            rules_help: Use rules to decide whether this shipping method can be applied to a given order.
                If a rule is true this shipping method can be applied. Notice that if you add multiple rules,
                all rules has to be true.
            tax_category: Tax category
            translations: Translations
        shipping_method_rule:
            items_total_greater_than_or_equal: Items total greater than or equal
            items_total_less_than_or_equal: Items total less than or equal
            type: Type
            total: Total
            total_weight_greater_than_or_equal: Total weight greater than or equal
            total_weight_less_than_or_equal: Total weight less than or equal
            weight: Weight
            amount: Amount
