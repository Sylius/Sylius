language: php

php:
  - 5.3
  - 5.4

before_script: composer install --dev --prefer-dist > /dev/null

script: bin/phpspec run -fpretty --verbose

notifications:
    irc: "irc.freenode.org#sylius-dev"
    email: "travis-ci@sylius.org"
