hwi_oauth:
    firewall_names: [shop]
    resource_owners:
        google:
            type: google
            client_id: 'google-client-id'
            client_secret: 'google-client-secret'
            scope: 'openid email profile'
