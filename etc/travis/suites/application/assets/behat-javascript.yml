imports: ["behat.yml.dist"]

default:
    extensions:
        Behat\MinkExtension:
            javascript_session: chromium
            sessions:
                chromium:
                    chrome:
                        api_url: "http://localhost:9222"
