#!/usr/bin/env bash

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../bash/packages.lib.sh"

clean_package="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/clean-package"

locate_packages | parallel -j100% --gnu "${clean_package} {}"
