#!/usr/bin/env bash

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../bash/packages.lib.sh"

validate_package="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/validate-package"

locate_packages | parallel -j100% --gnu "${validate_package} {}"
